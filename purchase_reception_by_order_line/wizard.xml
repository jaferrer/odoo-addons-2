<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <record model="ir.ui.view" id="stock_transfer_details_form">
            <field name="name">stock_transfer_details_form</field>
            <field name="model">stock.transfer_details</field>
            <field name="inherit_id" ref="stock_transfert_picking_type.stock_transfer_details_form_picking_type"/>
            <field name="priority" eval="16"/>
            <field name="arch" type="xml">
                <field name="item_reception_ids" position="attributes">
                    <attribute name="context">{'default_sourceloc_id':picking_source_location_id, 'default_destinationloc_id':picking_destination_location_id, 'default_group_name':picking_group_name, 'display_incoming_picking': True}</attribute>
                </field>
                <field name="item_ids" position="after">
                    <field name="picking_group_name" invisible="1"/>
                </field>
                <xpath expr="//field[@name='item_reception_ids']/tree" position="replace">
                    <tree string="Inventory Details Remaining Quantity" editable="bottom">
                        <field name="package_id" groups="stock.group_tracking_lot"/>
                        <field name="product_id" required="True" context="{'uom':product_uom_id}"
                               on_change="product_id_change(product_id,product_uom_id,context)"/>
                        <field name="group_name" invisible="1"/>
                        <field name="purchase_line_id" domain="[('product_id', '=', product_id), ('order_id.name', '=', group_name)]"
                               options="{'no_create': True, 'no_edit': True}"/>
                        <field name="quantity" string="Quantity received"/>
                        <button name="split_quantities" string="Split" type="object"
                                icon="STOCK_PREFERENCES"
                                attrs="{'invisible': [('quantity', '=', 1)]}"/>
                        <field name="product_uom_id" options="{&quot;no_open&quot;: True}"
                               groups="product.group_uom"/>
                        <field name="sourceloc_id"
                               domain="[('id', 'child_of', parent.picking_source_location_id)]"/>
                        <field name="destinationloc_id"
                               domain="[('id', 'child_of', parent.picking_destination_location_id)]"/>
                        <field name="result_package_id" groups="stock.group_tracking_lot"
                               context="{'location_id': destinationloc_id}"/>
                        <button name="put_in_pack" string="Pack" type="object" icon="terp-product"
                                attrs="{'invisible': [('result_package_id', '!=', False)]}"
                                groups="stock.group_tracking_lot"/>
                        <field name="lot_id" groups="stock.group_production_lot"
                               domain="[('product_id','=?', product_id)]"
                               context="{'product_id': product_id}"/>
                    </tree>
                </xpath>
            </field>
        </record>

    </data>
</openerp>